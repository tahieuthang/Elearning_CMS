auth('customer')->login(App\Models\Customer::find(1)); $resSubmit = app(App\Services\CourseServices::class)->submitQuiz(8, ['isPassed' => true]); print_r($resSubmit); $resDetail = app(App\Services\CourseServices::class)->getCourseById(1); $curriculum = $resDetail->curriculum; foreach($curriculum as $c) { if ($c->type === 'quiz') { echo 'Quiz ID: ' . $c->id . ', is_passed: ' . ($c->is_passed ? 'true' : 'false') . PHP_EOL; } }
