# Mandatory Claude Code instructions

You MUST read `docs/ai/PROJECT_RULES.md` completely before searching, running
commands, planning, reviewing, or changing any file in this repository.

The following constraints are non-overridable:

- Do not commit, push, merge, rebase, tag, publish, or create pull requests.
- Preserve all pre-existing user changes and keep the task narrowly scoped.
- Do not expose secrets or production data.
- Inspect Git status before editing and report only checks actually executed.

Also read any scoped `AGENTS.md` or `CLAUDE.md` applicable to the target path.

